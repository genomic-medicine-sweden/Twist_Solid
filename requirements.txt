setuptools<70.0.0
# TODO: pin to a hydra-genetics PyPI release once one with Snakemake 9 / Python 3.12 support
# exists. It must be >= 4.1.1, since common.smk calls hydra_min_version("4.1.1"); the migration
# branch currently resolves to 3.4.1.dev34, which is why that call is temporarily relaxed.
hydra-genetics @ git+https://github.com/hydra-genetics/hydra-genetics.git@migrate-to-snakemake9-python3.12
jinja2>=3.1.0
numpy>=1.24.0
pandas>=1.3.1
pyaml-env>=1.2.2
pysam
scipy
snakemake>=9.0.0,<10
tabulate<0.9.0
